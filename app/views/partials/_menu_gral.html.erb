<!-- Navigation Bloc -->
<div class="bloc bgc-charcoal d-bloc sticky-nav" id="nav-bloc">
  <div class="container bloc-sm">
    <nav class="navbar row">
      <div class="navbar-header">
        <%= link_to root_path do %>
            <%= image_tag "https://s3-us-west-2.amazonaws.com/cuantopido/loguito.png", :width => "200", :alt => "logo", :class => "logo_head" %>
        <% end %>

        <button id="nav-toggle" type="button" class="ui-navbar-toggle navbar-toggle" data-toggle="collapse" data-target=".navbar-1">
          <span class="sr-only">Toggle navigation</span><span class="icon-bar"></span><span class="icon-bar"></span><span class="icon-bar"></span>
        </button>
      </div>
      <div class="collapse navbar-collapse navbar-1">

        <ul class="site-navigation nav navbar-nav">
          <li>
            <div class="dropdown borde_derecha borde_izquierda">
              <a id="pl-1597" class="dropdown-toggle" href="#" data-toggle="dropdown" aria-expanded="false">Servicios<span class="caret"></span></a>
              <ul class="dropdown-menu bgc-charcoal" role="menu">
                <li><%= link_to "Todos", services_path %></li>
                
              </ul>
            </div>
          </li>
          <li>
            <div class="dropdown borde_derecha">
              <a id="pl-1597" class="dropdown-toggle" href="#" data-toggle="dropdown" aria-expanded="false">Categorías<span class="caret"></span></a>
              <ul class="dropdown-menu bgc-charcoal" role="menu">
                <li><%= link_to 'Todos', products_path %></li>
                <% @my_categoryes.each do |c| %>
                    <li><%= link_to "#{c.category}", controller: "products", category: "#{c.category}" %></li>
                <% end %>
              </ul>
            </div>
          </li>
          <li>
            <% if admin_signed_in? %>
                <%= link_to 'Salir', destroy_admin_session_path, :method => :delete, :class => "borde_derecha" %>
                <% else %>
                  <% if user_signed_in? %>
                  <% else %>
                    <%= link_to 'Registrate', new_user_registration_path, :class => 'borde_derecha'  %>
                  <% end %>
                </li>
                <li>
                  <% if user_signed_in? %>
                      <div class="dropdown borde_derecha">
                        <a id="pl-1597" class="dropdown-toggle" href="#" data-toggle="dropdown" aria-expanded="false">Mi Cuenta<span class="caret"></span></a>
                        <ul class="dropdown-menu bgc-charcoal" role="menu">
                          <%= link_to 'Editar Datos', edit_user_registration_path %>
                          <%= link_to 'Salir', destroy_user_session_path, :method => :delete  %>
                        </ul>
                      </div>
                  <% else %>
                    <%= link_to "Login", "#login", "data-toggle" => "modal", "data-target" => ".bs-example-modal-sm", :class => 'borde_derecha' %>
                <% end %>
            <% end %>
          </li>
        </ul>
      </div>
    </nav>
  </div>
</div>